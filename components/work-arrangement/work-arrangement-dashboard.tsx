import WorkArrangementLineChart from "./work-arrangement-line-chart";

export default function WorkArrangementDashboard() {
    return (
        <div className="flex flex-col gap-4">
            <WorkArrangementLineChart></WorkArrangementLineChart>
        </div>
    )
}